{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "go build -o ${workspaceFolder}/bin/wkmailsync ${workspaceFolder}/cmd/wkmailsync",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": "$go"
        }
    ]
}
